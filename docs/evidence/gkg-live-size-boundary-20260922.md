# GKG 真实批次大小边界与恢复验证

状态：**有界解析离线验证通过；正式调度恢复待提交后复核。** 本报告只证明一个真实批次能在明确上限内解析，不证明未来七天的批次大小、五议题召回率或历史覆盖完整。

## 触发原因

专用 M4 准备库中的五个 GKG 任务真实请求官方 `lastupdate.txt` 后失败。旧上限为压缩 4 MiB、解压 16 MiB；官方 `20260922150000.gkg.csv.zip` 实际超过两者。失败、重试、请求计数和零入库均保留，没有改写成空结果或成功。

## 精确重放

| 项目 | 实测 |
|---|---|
| 官方批次 | `20260922150000` |
| 压缩大小 | 6,578,603 B |
| 解压 CSV | 20,389,639 B；单文件 `20260922150000.gkg.csv` |
| MD5 | `ad89d137d38429d21bbc14bae930d182` |
| SHA-256 | `5cd56927db5bbe6d2a3918044df8b5b719379df6f7a05dcfcae92ca2afec0399` |
| 当前代码上限 | 压缩 8 MiB；解压 32 MiB |
| `parse_zip` 结果 | 1,549 条元数据 |
| JSON 缓存大小 | 1,307,616 B |

现有单文件、固定文件名、HTTPS 固定域名、索引大小、MD5、加密/符号链接、压缩与解压上限仍保留。32 MiB 是输入字节上限，不是 Python 堆内存的严格上限；未来可另加行数和单行长度保护。

## 五议题离线匹配

同一真实批次按已保存关键词离线匹配：联合国大会 4 条；Fed、半导体出口管制、加沙、红海/霍尔木兹均为 0。零匹配不能解释为现实中无变化；GKG 仅最新批次、发布时间未知且元数据召回受关键词限制。若七天内需要调整关键词，必须作为人工纠错留下旧/新版本和理由。

## 验证边界

- 一个批次通过不保证后续批次低于 8/32 MiB；未来超限应按真实失败记录，不能自动放宽。
- 解析成功只证明传输、校验和元数据结构可用，不证明语义相关性或达到 50 条材料门槛。
- 正式恢复需在干净提交上以原数据目录和原端口启动调度器，不删除 retry/cache，不重建任务；恢复后补记提交、来源/任务状态、请求增量和 `source.recovered`。

## 自动回归

`2026-09-22` 执行 `python3.11 -m unittest discover -s tests -v`：274 项，41.455 秒，全部通过。新增回归同时覆盖观测/变化正确字段、运行提交与本机健康身份、T0 防回填、start 快照绑定、168 小时结束门、精确引用闭包、历史版本窗口、脏工作树阻断和不可覆盖输出。自动测试不替代真实七天观察。
