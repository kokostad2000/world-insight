# 最终整合验证

日期：2026-09-21。整合基线为 `ea1abde818d18db0fbc2cbf9762a30e47b666e20` 加本报告同批工作区改动；最终提交号在提交后由 `git log -1` 确认。环境为 macOS 15.6.1 arm64、Apple M1、Python 3.11.4、SQLite 3.42。本批没有外部网络请求、GitHub 推送、真实研究库写入或付费/AI调用。

## 完整回归

命令：

```bash
python3 -m unittest discover -s tests -v
```

最终命令结果：**272 tests，41.517 秒，全部通过**。测试包含纯领域、隔离 SQLite、实际回环 HTTP、隔离本机进程、备份恢复与更新回退；夹具、故障注入和模拟时钟仍按各用例标签解释，不能当作真实上游、真实睡眠或七天观察。

新增的关键闭环包括：

- 同一来源记录跨周再次成功收取，只更新无正文的 `evidence_acquisition` 凭据；不改 Evidence v1、首次采集、发布时间、变化或“今天发生”统计。
- 采集成功路径在 Evidence 与收取凭据同一事务中完成；失败回滚和 304 无材料不会推进最近收取时间。
- 恢复后的来源约束在写入前和备份/恢复渠道执行，`store=false`、仅元数据或已到期内容不会暂存于当前或历史版本；标题、身份、链接、笔记和研究引用保留。
- 来源列表兼容开发期旧容量库中缺少预算字段的局部记录，返回预算默认值而不产生 500。

## 正式浏览器

- [跨周重复收取](browser-temporal-acquisition.md)：正式 Scheduler 离线响应两次，页面分列发布时间、首次采集和最近收取；材料及研究变化没有伪造新版本。
- [恢复后来源限制](browser-policy-recovery-final.md)：来源页保留恢复约束，重新核对复选框默认未选；材料页隐藏正文并保留固定版本和人工笔记。
- [最终浏览器容量](browser-performance-final.json)：20,000 材料、2,500 说法、2,500 事件、10 议题、5 并发标签页、40 样本，首个可用内容 P95 703.5 毫秒，40/40 DOM 有效，控制台 0 错误。

浏览器页面证明记录的 UI 和本地读取行为；不证明研究内容语义正确、真实上游完整、其他硬件表现或连续运行。

## 容量与真实材料门槛

- [最终 API 容量](policy-performance-final-integrated.json)：常用筛选 P95 0.360 秒、搜索 P95 0.489 秒、完整 bundle P95 1.629 秒。PRD 的常用筛选 API 1 秒门槛通过；bundle 单列，不冒充筛选门槛。
- [10 条官方材料](official-material-review.md)：正式 HTTP 人工登记、逐条读取与 history 共 42 次本机请求全部 200；8 条 Fed 当前官方页核对，2 条 WDI 只核对既有真实 API 缓存且保留本次页面失败。10 条均为元数据授权、无正文、无自动判断。

## 仍未完成

- AC-26 Finder 双击受当前辅助功能/录屏权限阻挡；终端中文/空格路径、重复启动和停止已通过。
- AC-36 真实 Mac 睡眠/唤醒未执行；进程锁和模拟时钟缺口逻辑不能替代实际睡眠。
- 5 个真实议题连续 7 天、50 材料/20 事件说法/10 判断结构抽查及真实用户约 10 分钟效率记录尚未开始或完成。PRD M3 因 AC-26/36 仍不能宣告完成，M4 保持待观察。
