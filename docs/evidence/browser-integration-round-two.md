# 主线第二轮浏览器及 Mac 集成验收

日期：2026-09-21。macOS 15.6.1 arm64、Python 3.11.4、SQLite 3.42.0。主线正式 app 页面，Codex 内置浏览器；未使用临时 UI harness。数据与端口完全隔离，下面明确区分真实来源副本与人工测试夹具。

## 运行中备份、空目录恢复、正常更新及失败回退

中文与空格代码目录 `/private/tmp/World Insight 中文 QA-znmxrdsi/全新代码 repo`，数据目录同父级 `恢复资料 Data`，端口 8876。原始真实来源库并未被替换。

1. dd527ab 新代码目录从运行中的原实例备份恢复，旧议题、材料引用可读，浏览器保存判断 v2→v3；议题国家与来源设置保存 v1→v2。前批步骤见 `browser-integration.md`、`mac-clean-restore.json`。
2. 仅从本地根仓库 fetch ad5e32f，工作区干净，执行 update dry-run 和 update，均退出0，目标 release/data/schema 验证通过。见 `mac-normal-update.json`。
3. 更新后浏览器打开原议题 `7b3852f6-ae75-4a48-81d5-2a4e13ce60fe`，当前判断 v3 及固定材料 `8d46566f-19e0-412b-9351-b045c95d5f92@2` 可见。修改“修订原因”后保存为 v4，材料引用保持v2。
4. 点击“导出研究→下载JSON”。工具等待 download 事件超时，但真实文件 `/Users/zhanglike/Downloads/world-insight-7b3852f6-ae75-4a48-81d5-2a4e13ce60fe.json` 已落盘，276673字节，SHA256 `f22b2405ffd7084c0112683d12f8ed058403e01da1a64af01e62329d9e8dda8b`。解析确认正确议题、判断v4、固定材料v2。独立Edge的JSON/Markdown产物比对见 `../handoffs/lifecycle.md`；事件超时不再被视为下载失败证据。
5. 仅在这个隔离代码副本创建本地故障提交 `6434834c31f6ebe9fd0db6a1227dca903df146a3`：schema2先建测试表，再执行无效SQL。主仓库迁移未改。切回干净ad5e32f后执行真实更新，CLI按错误约定退出1，更新记录为 rolled_back，恢复ad5e32f/schema1，半迁移表不存在、维护标记清除。见 `mac-migration-failure.json`。
6. 故障回退后浏览器读取判断v4及原固定材料v2，再修改修订原因并保存为v5。页面明确显示版本5，引用仍v2。不是仅健康检查通过。

故障验证脚本曾把预期失败退出码误写作2，导致脚本末尾断言失败；实际产品返回约定的1并成功回退。保留该测试脚本错误说明，产品结论依据更新日志、只读数据库核对及随后真实浏览器保存，而非隐去错误或重新编造运行结果。

发现：WDI在此前采集实现中将CHN/USA都关联到USA议题，导致背景多出CHN。已提交修复任务，bd8162a增加请求/响应国家交集及全局缓存；bundle过滤由权限接线一并修。此处不能将修复前页面认定为国家范围验收通过。

## 正式主线删除、恢复及全库搜索

数据 `/private/tmp/world-insight-root-ui-irsbcc3o`，端口8878，`--no-scheduler`；全是明确命名“隔离验收”的fixtures，没有外部来源请求。具体IDs见 `root-ui-fixture.json`。页面基于ad5e32f及随后22d2b89前端接线。

1. 材料详情 `140b1327-3c19-4bba-b6ad-5f48dc3d969c` 的“移入回收站”打开影响预览：明确列出引用判断和已有变化记录，各自版本保持。
2. 下一步实际生成并核验 `deletion-5ed975da-498f-4714-81ea-660fd85d3761.wibackup/.json`，显示本地完整路径和1个历史正文因export:false省略。
3. 填写隔离验收原因、勾选确认后，材料进入正式“本地运行”页面回收站v2；原判断成为needs_review。
4. 经正常“恢复”表单填写原因确认，回收站变0项，材料恢复v3，人工笔记仍在。首页仍显示原判断“待重审”，没有自动完成重审。
5. 首页另一项过期待复查情景点击“审阅情景”，正确打开情景编辑器，显示相应情景字段，未再错误请求judgments路由。
6. “我的判断”显示实际204条总数、1条待重审。搜索最早一批“隔离分页判断 000”，点击明确搜索按钮后显示1条且记录可操作，证明覆盖200条以外记录。输入框亦支持Enter提交；此前仅依赖blur/change在该浏览器工具填值后未触发刷新，已补显式提交入口并实测。

边界：这些是正式页面与真实SQLite的隔离行为验证，不是真实国际事件、七天观察或政策结论复核。相似材料独立模块测试见其handoff；正式主线点击流程另行补验。Finder仍缺电脑控制权限，未执行双击，不以终端入口或浏览器操作替代。


## 主线相似材料检查追加

同一8878正式详情页中新增独立候选 `a99cec59-5740-4c81-965e-2b68b341a3c3`（example.org链接只是隔离测试标识，未抓取外部正文）。页面显示1条标题相似建议，并解释不同日期/地点不自动合并、选择仅登记转引。实际点击核对→填写测试理由→确认，原材料v3→v4；明确移除关联为v5；查看关联历史，再核对恢复v4关系为v6。当前页面仍有两个独立材料。只读核对记录与说明如下。

材料当前版本6，原稿关联a99cec59-5740-4c81-965e-2b68b341a3c3；人工笔记仍为“人工笔记应保留”。库内数量：材料2、事件0、判断204。未从相似候选自动生成事件或合并记录。


## 阅读状态随迁移失败回退保留

在8876真实资料恢复副本，浏览器选择“自上次阅读”，点击“将本页已展示变化标为已读”，写入20条固定变化版本阅读状态，剩余36条未读。仅暂停这个恢复副本的来源采集以隔离对照，原8766实例未改。随后再次以既有故障目标执行更新，CLI退出1、记录rolled_back。`mac-reading-rollback.json`保留20条read_state前后完整相等的清单、更新日志和校验结果；浏览器重新打开首页仍选择“自上次阅读”，显示36条未读。此前回退后已实际保存判断v5。这补充PRD12.2的阅读进度对照。

`mac-update-network-failure.json`另记录README取代码阶段的真实Git请求失败：只对该命令指定不可达本地代理，GitHub fetch退出128，原程序选择、研究记录指纹不变。未改系统网络，不能称整机断网验收。

## 精确语义与全量复盘页面

2026-09-21 10:33—10:35（Asia/Shanghai），同一8878正式页面、明确隔离样本。IDs见`root-ui-semantics-fixture.json`。

- 资料库搜索“隔离转载计数”：11条材料（10篇同链报道加1条未知），已识别来源链1，独立性未确认1，明确说明来源链数量不等于事实证实次数。未合并10篇记录。
- 议题时间线只有1条明确标记测试的争议事件，分别展示甲来源称5人、乙来源称12人，两个不同材料ID的v1链接，“存在分歧”，没有合成单值。事件发生为2026-09-14，首页发现时间为2026-09-21。
- “甲方宣布停火；尚未核实是否实施”位于已登记说法，显示待核查、主体归属及“说法不等于已证实事实”，没有增加停火实施事件。
- 已审阅的假设判断在议题当前判断、首页变化（新增及修订两条）仍显示“假设型暂定判断／缺乏证据支持”；首页关注卡片亦明确缺乏证据支持。
- 我的判断：205条判断、已有复盘202、结果无法判定202，注明“单独记录，不计为预测成败”。实际逐次点击复盘“下一页”，检查第2至11页均为共202条；第11页显示复盘001、000两个最早记录，固定判断v1，下一页禁用。判断分页保持第1页，两个列表互不串页。

这些是正式页面行为验证，样本不代表真实国际事件或观察结果。新的来源权限后端路由合入后仍需独立读取/搜索/导出验证；本段不代替该项。


## 来源收紧后的历史、搜索与实际导出

8878 隔离正式页面，主线权限路由已合入；标识及真实下载文件指纹见 `browser-rights-fixture.json`。先登记允许保存/展示/导出的来源、材料v1与固定v1判断，再改材料笔记形成v2。页面来源设置取消展示和导出后，材料v3与判断待重审出现。打开材料历史v1及全部历史快照：正文标识 `RIGHTS_BROWSER_90814` 和译文标识 `RIGHTS_TRANSLATION_90814` 均不在DOM文本中；人工笔记、原链接、固定v1引用保留。搜索正文标识返回0，搜索笔记返回1。

实际点击JSON与Markdown导出，两文件落在Downloads，分别24555与17430字节；解析/文本核对两项正文标识均不存在，人工笔记与固定v1存在。hash及文件绝对路径保留在证据JSON。该结果覆盖普通读取/导出权限，不代表旧备份恢复后权限事实已持久；后者发现独立缺陷并另修。

## 空目录恢复后的自动浏览器与阅读状态

`mac-browser-readstate-restore.json` 记录保留的真实Mac恢复夹具，程序和资料目录均含中文/空格。root实际运行start.sh，未传no-browser，健康成功后Edge自动打开8877并渲染首页（02:43:57.478Z）。随后在独立内置浏览器查看同一实例：全部变化7条，选择“自上次阅读”只剩5条，与恢复的2条read_state吻合。打开旧议题编辑判断v2→v3，修订理由成功保存，固定材料v1不变，历史v1可读。

再次从磁盘核对3个合法自编附件大小和SHA256，包括升级后新增附件，全部与被拒回退时保留的备份一致。正常更新后新写入、拒绝不安全回退、取其当前恢复包恢复到空目录的前半流程见 `../handoffs/mac-acceptance-remaining.md`。这证明终端入口自动打开浏览器；仍未证明Finder双击或真实OS睡眠。

## 正式 GKG 真实库浏览器检查

2026-09-21 11:03起，root以正式server.app启动8880，`--no-scheduler`，数据为 `gkg-live-scheduler.md` 两次真实请求留下的SQLite；没有额外采集请求。来源页面显示GKG部分覆盖、上次成功10:55、免费/共享请求2/2、最小间隔6秒，并明确仅最新批次、批次时间非发布时间、历史未补齐。DOC/RSS/WDI在这个专用实例停用，AI/付费未配置。

资料库显示45条材料，标题、原URL域名、待核查、发布时间未知、采集10:55；元数据许可的材料显示正文展示受限。抽查列表中的时装标题说明上游主题命中可能不相关，不能把关键词召回写成事实或相关性验收。没有自动创建说法、事件或判断。

来源任务表已在正式8880页面追加复核：三行均显示“本次任务完成”，同时逐行显示GKG最新批次未补齐历史的完整原因。任务成功不再用整体覆盖完成文案。

AC20补验：正式8880来源页明确航空/船舶等预留能力未接入、没有模拟轨迹；点击“地图与国家”，实际静态底图显示无航空或船舶轨迹、暂无事件位置、只有真实登记的位置才会显示。未把空资料说成现实无飞机/无船。
